# .env 配置模板 —— 复制为 .env 后填入真实值（.env 已被 gitignore，不会提交）
# 本文件仅为模板，禁止填入任何真实密钥

# DeepSeek API Key（AI 助手 / 业务分析功能使用）
# 获取地址：https://platform.deepseek.com/
# 格式为 sk- 开头的一串字符
AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 说明：
# 1. 本地直接运行 backend（IDE 或 mvn spring-boot:run）时，application.yaml
#    会读取项目根目录下的 .env 文件（spring.config.import 导入）
# 2. 使用 docker-compose 部署时，.env 位于仓库根目录，
#    compose 会将 AI_API_KEY 以环境变量形式注入容器（优先级更高）
